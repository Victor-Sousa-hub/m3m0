{
  "name": "m3m0-sync",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --persist-to=./.wrangler/state",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "migrate:local": "wrangler d1 migrations apply m3m0-sync-db --local",
    "migrate:remote": "wrangler d1 migrations apply m3m0-sync-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "~6.0.3",
    "wrangler": "^4.0.0"
  }
}
